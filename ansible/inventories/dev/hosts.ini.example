[homelab_managers]
dev1 ansible_host=<IP-DO-CT-DEV> ansible_user=nuvidio

# ⚠️  NUNCA popule este grupo em dev. [homelab_workers] tem que ficar VAZIO.
# `02-bootstrap-cluster.yml` roda `docker swarm join` na hora, sem confirmação,
# em todo host deste grupo. Um inventário de dev errado (ex.: copiar node2 do
# prod) juntaria o CT de dev ao Swarm de PRODUÇÃO.
[homelab_workers]

# Consumido só por `playbooks/00-node-bootstrap.yml`, no primeiro contato com
# o CT 210 recém-criado — antes de o usuário `nuvidio` existir no host, então
# o acesso ainda é via `root`. Depois do bootstrap, o resto dos playbooks usa
# `nuvidio` via [homelab_managers] acima.
[dev_bootstrap]
dev1 ansible_host=<IP-DO-CT-DEV> ansible_user=root

[opnsense]
# Em dev não há dispositivo de rede. Grupo fica presente e vazio (comentado)
# porque `groups['opnsense']` ausente explode o template do Prometheus.
# opnsense ansible_host=<IP-DA-OPNSENSE> ansible_user=root

[openwrt]
# openwrt ansible_host=<IP-DO-OPENWRT> ansible_user=root ansible_shell_type=sh

[proxmox]
# hypervisor1 ansible_host=<IP-DO-HYPERVISOR-1> ansible_user=root
# hypervisor2 ansible_host=<IP-DO-HYPERVISOR-2> ansible_user=root

[omv]
# omv ansible_host=<IP-DO-OMV> ansible_user=root

[homelab:children]
homelab_managers
homelab_workers

[network:children]
opnsense
openwrt
